package example:web;

interface http {
    record request {
        method: string,
        path: string,
        body: list<u8>,
    }

    record response {
        status: u16,
        body: list<u8>,
    }

    handle: func(request: request) -> response;
}

world web {
    export http;
}
