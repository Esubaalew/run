# Session demo:
# run --lang julia --code "counter = 0"
# run --lang julia --code "counter += 1"
# run --lang julia --code "counter"

using Printf

msg = "Julia sessions make iterative experiments easy."
println(msg)
