name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  check:
    name: Check & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - name: cargo fmt
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: cargo check
        run: cargo check --all-targets

  check-v2:
    name: Check & Lint (v2)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: cargo clippy (v2)
        run: cargo clippy --features v2 --all-targets -- -D warnings
      - name: cargo check (v2)
        run: cargo check --features v2 --all-targets

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable

      - name: Install language toolchains (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 nodejs golang-go

      - name: cargo test
        run: cargo test --lib --tests
        timeout-minutes: 15

  test-v2:
    name: Test v2 (ubuntu)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable

      - name: cargo test (v2)
        run: cargo test --features v2 --lib --tests
        timeout-minutes: 15

  build:
    name: Build release
    runs-on: ubuntu-latest
    needs: [check, test, check-v2, test-v2]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: cargo build --release
        run: cargo build --release
      - name: Binary size
        run: ls -lh target/release/run
